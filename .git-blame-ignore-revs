# Commits listed here are ignored by `git blame` (and by GitHub's blame view,
# which reads this file automatically). Use `git blame --ignore-revs-file
# .git-blame-ignore-revs` locally, or configure it once with:
#   git config blame.ignoreRevsFile .git-blame-ignore-revs

# SLING-13269: sling-bundle-parent 43 -> 66 upgrade; mvn spotless:apply
# reformat of every source file, kept separate from the actual
# dependency/config changes.
16cf05496dbd56cd5308ec2345849998ae554517
